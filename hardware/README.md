# Hardware

Esta carpeta contiene la documentación y el código del dispositivo ESP32 de VitalBand.

- `firmware/` — firmware principal y credenciales.
- `tests/` — sketches de prueba por sensor y modo test.

Consulta `hardware/firmware/README.md` para la guía completa de instalación y configuración.
